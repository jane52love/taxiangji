# 她乡记 · 使用说明（金梅姐看这里）

## 两种使用方式

**方式 A · 手机页面（推荐日常）：** 在 Trae 里启动桥接服务，手机浏览器打开页面（可添加到主屏幕），像聊天一样说，成稿自动存进项目文件夹。

**方式 B · 直接在 Trae 对话：** 不打开页面，直接在 Trae 对话框里说需求也可以，效果一样。

## 启动手机页面（方式 A，两步）

1. 在 Trae 的终端里运行：`python3 bridge/server.py 4179`（首次会自动生成 HTTPS 证书）
2. 手机和电脑连同一个 WiFi，手机浏览器打开：`https://电脑IP:4179`（服务启动时会打印具体地址；首次打开出现证书警告时，点「高级」→「继续访问」——这是自签证书的正常提示，语音识别必须 HTTPS）
   - 查电脑 IP：终端输入 `ipconfig getifaddr en0`
   - 菜单里选「添加到主屏幕」，以后像 App 一样点开

## 第一次使用

### 第 1 步：把文件夹放到电脑上

把收到的「她乡记」文件夹解压，放到一个好找的位置，比如桌面。

### 第 2 步：用 Trae 打开这个文件夹

1. 打开 Trae（没有就先在官网下载安装）
2. 打开文件夹：选择解压出来的「她乡记」文件夹
3. 这样 Trae 就知道你的素材库、知识库都在哪了

### 第 3 步：导入 Skill（一次即可，以后不用再导）

1. 在 Trae 中找到「Skill / 技能」管理入口
2. 选择「导入」，依次选择 `skill安装包/` 文件夹里的 3 个 zip：
   - `mulan-video-content-v5.zip`（她乡记文案，主力）
   - `对标账号分析-skill.zip`（对标账号文案）
   - `account-diff-compare-skill.zip`（对标账号分析）
3. 导入完成后就能用了

> 就算不导入 Skill 也能用：助手会按项目规则读取 `skills/` 里的定义照常工作。

## 用手机 Trae 体验

本项目是一个完整的 Trae 项目（规则、知识库、素材都在文件夹里），手机 Trae 打开后直接对话即可，助手的一切规则和知识都在项目里。

### 方式一：Trae 云端（电脑可以关机）

1. 项目需要先推到 GitHub 仓库（Trae 云端任务基于 GitHub）
2. 手机 Trae 中新建任务时，运行环境选「云端」，Code 模式选择本项目的仓库和分支
3. 直接对话创作；助手归档的素材和知识库修改会自动提交回仓库

### 方式二：连接我的电脑（素材实时落本地文件夹）

1. 把「她乡记」文件夹放到电脑「文稿（Documents）」目录下（Trae 移动端只能访问已授权的目录）
2. 电脑 Trae：设置 → TRAE 移动端 → 打开「允许 TRAE 移动端控制当前设备」
3. 手机 Trae：对话页左下角运行环境选「连接我的电脑」→ 选择她乡记文件夹
4. 手机说话、传照片，助手在电脑上干活，素材直接归档到项目文件夹

> 两种方式都支持语音、图片上传和素材归档。方式二同时保留了 PWA 页面联调能力（电脑开机时）。

**语音识别（按住说话）：** 手机上「按住说话」走云端转写，需要在 `桥接/config.json` 里配置一次识别服务（复制 `桥接/config.example.json` 为 `config.json`，填入 API Key 即可，推荐硅基流动的 TeleSpeechASR，按秒计费很便宜）。没配置时自动降级：电脑 Chrome 可以直接用，手机则建议用文字输入或手机输入法自带的语音转文字。

## 日常怎么用

### 写文案（最常用）

打开对话，像跟人聊天一样说：

> 我今天路过一个人家的院子，被他家院子里晒的老柜子吸引到了……（说说你看到了什么、听到了什么）

也可以直接发照片。然后：

- 助手会先问你几个问题（把你说不清的内容挖出来），再写文案
- 满意后跟助手说「保存」，它会自动存到 `我的素材/日期-标题/` 文件夹里（文案 + 你发的图片都在里面）
- 不满意就说哪里不满意（比如「开头不够抓人」），助手会先给几个改法让你挑

### 分析对标账号

> 帮我对比这两个账号：链接1 + 链接2

会生成一份可视化的对比报告。

### 学对标账号写文案

> 我想参考「某某账号」的风格帮我写一篇……（附上账号链接或截图）

## 常见问题

**问：文案存到哪里了？**
答：都在「我的素材」文件夹里，一个主题一个文件夹（日期-标题命名）。

**问：想改「说话风格」或加新主题怎么办？**
答：让助手改就行，比如「帮我把『打糍粑』这个主题加进场景触发库」，它会帮你更新 `我的知识/` 里的文件。

**问：换电脑/重装了怎么办？**
答：把整个「她乡记」文件夹拷走，在新电脑上重新做一次「第一次使用」的 3 步即可，素材和知识都在文件夹里，不会丢。
